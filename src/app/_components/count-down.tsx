// import dayjs from "dayjs";
// import relativeTime from "dayjs/plugin/relativeTime";

// dayjs.extend(relativeTime);

// export default function Countdown(props: { props: { passedDate: string } }) {
//   return (
//     <div>
//       {JSON.stringify(props)}
//       {/* New Year: {dayjs(props.passedDate).fromNow(true)} New Year:{" "} */}
//       year {dayjs().diff(props.passedDate, "year")}
//       <hr />
//       month {dayjs().diff(props.passedDate , 'month')}
      
//       <hr />
//       day {dayjs().diff(props.passedDate , 'day')}

//       {/* day {dayjs(props.passedDate).fromNow(true)} remaining */}
//     </div>
//   );
// }
